# Train model locally

Train model locally

## Usage

``` r
train_model_local(
  data_dir,
  model_id,
  model_variant,
  resolution,
  epochs,
  batch_size,
  grad_accum_steps,
  learning_rate,
  device,
  workspace_dir,
  use_amp,
  amp_dtype,
  gradient_checkpointing,
  num_workers,
  validate_every,
  early_stopping_patience
)
```
