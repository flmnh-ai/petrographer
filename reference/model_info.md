# Get model info

Get model info

## Usage

``` r
model_info(model_id, board = NULL)
```

## Arguments

- model_id:

  Model name

- board:

  Pins board (NULL = public hub, "local" = local training board)
