% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prediction.R
\name{confusion_matrix}
\alias{confusion_matrix}
\title{Compute a confusion matrix from SAHI predictions + COCO ground truth}
\usage{
confusion_matrix(
  predictions,
  annotation_json,
  iou_threshold = 0.5,
  score_threshold = 0
)
}
\arguments{
\item{predictions}{Tibble of predictions with \code{image_id}, \code{category_id},
\code{score}, \code{xmin}, \code{ymin}, \code{width}, \code{height} (the \code{predictions} element of
\code{\link[=evaluate_model_sahi]{evaluate_model_sahi()}} output works directly).}

\item{annotation_json}{Path to the COCO annotation JSON the predictions
were produced against.}

\item{iou_threshold}{Minimum IoU for a prediction to count as matching a
GT box (default 0.5).}

\item{score_threshold}{Filter predictions below this score before matching
(default 0; keep all).}
}
\value{
A list with:
\itemize{
\item \code{long}: long-format tibble \verb{(gt_label, pred_label, count)}
\item \code{matrix}: wide tibble, one row per GT label
\item \code{class_names}: character vector of class labels (includes "background")
\item \code{iou_threshold}: the threshold used
}
}
\description{
For each image, greedily match predicted boxes to ground-truth boxes by IoU
(highest-scoring prediction matches first). Predictions that match a GT box
with IoU >= \code{iou_threshold} contribute a (gt_class, pred_class) cell.
Unmatched predictions land in the "background" \emph{row} (false positives);
unmatched GT boxes land in the "background" \emph{column} (false negatives).
}
\details{
Detection doesn't have a clean confusion-matrix definition (unmatched
predictions and unmatched GT don't map onto classification confusion
cleanly), so treat this as a diagnostic heatmap rather than a calibrated
metric.
}
