% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/align.R
\name{align_images}
\alias{align_images}
\title{Align thin section image pair}
\usage{
align_images(ppl_path, xpl_path, output_path, method = "similarity")
}
\arguments{
\item{ppl_path}{Path to PPL image}

\item{xpl_path}{Path to XPL image (reference)}

\item{output_path}{Path to save aligned PPL image. If a directory, generates
filename from input.}

\item{method}{Alignment method: "similarity" (rotation+scale+translation with
automatic fallback to translation if checks fail) or "translation"
(translation only, no rotation/scale)}
}
\value{
List with n_matches and method_used (invisibly)
}
\description{
Aligns a PPL (plane-polarized light) image to an XPL (cross-polarized light)
reference image using SIFT feature matching. Designed for images of the same
slide taken after remounting (e.g., adding polarizing filters). Automatically
validates that scale and rotation are reasonable and falls back to
translation-only if needed.
}
