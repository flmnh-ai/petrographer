% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prediction.R
\name{evaluate_model_sahi}
\alias{evaluate_model_sahi}
\title{Evaluate detections with SAHI and COCO metrics}
\usage{
evaluate_model_sahi(
  model,
  annotation_json,
  image_dir = NULL,
  use_slicing = TRUE,
  slice_size = 512,
  overlap = 0.2,
  max_images = NULL,
  save_predictions = NULL,
  iou_type = "bbox",
  max_dets = 100
)
}
\arguments{
\item{model}{A \code{PetrographyModel} from \code{\link[=load_model]{load_model()}}.}

\item{annotation_json}{Path to COCO annotation JSON (e.g. \verb{valid/_annotations.coco.json}).}

\item{image_dir}{Directory containing the images referenced in the
annotation file. If \code{NULL}, image paths are resolved relative to the
annotation file.}

\item{use_slicing}{Whether to use SAHI sliced inference (default \code{TRUE}).}

\item{slice_size}{Slice size for SAHI inference (pixels, default 512).}

\item{overlap}{Overlap ratio between slices (default 0.2).}

\item{max_images}{Optional maximum number of images to evaluate (useful for
smoke tests).}

\item{save_predictions}{Optional path to write COCO-format predictions JSON.}

\item{iou_type}{IoU type to evaluate (\code{"bbox"} by default).}

\item{max_dets}{Maximum detections per image for evaluation. For dense detection
(100+ objects), set to 300 or higher (default: 100).}
}
\value{
A list with elements \code{summary} (tibble of COCO metrics),
\code{predictions} (tibble of detections), and \code{coco_eval} (pycocotools object).
}
\description{
Runs SAHI inference across the validation set described by a COCO-style
annotation file and computes COCO metrics using \code{pycocotools}. The function
returns a tidy summary of the standard 12 bbox metrics alongside the raw
prediction table for further analysis.
}
