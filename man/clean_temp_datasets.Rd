% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dataset.R
\name{clean_temp_datasets}
\alias{clean_temp_datasets}
\title{Delete auto-pinned temp datasets}
\usage{
clean_temp_datasets(board = NULL, confirm = interactive())
}
\arguments{
\item{board}{Pins board (\code{NULL}/\code{"local"} = local dataset board).}

\item{confirm}{If \code{TRUE}, prompt before deleting. Defaults to \code{TRUE} in
interactive sessions, \code{FALSE} otherwise (e.g. scripted cleanup).}
}
\value{
Character vector of deleted dataset ids (invisibly).
}
\description{
When \code{\link[=train_model]{train_model()}} is called with \code{data_dir} rather than \code{dataset_id}, it
auto-pins the dataset as \verb{_temp_<timestamp>} (tagged \code{temp = TRUE} in
metadata) so training is reproducible. Those pins persist in
\verb{.petrographer/datasets/} and accumulate over time — each is a tar.gz of
the full dataset. This helper removes them.
}
