% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{get_training_dataset}
\alias{get_training_dataset}
\title{Get the dataset used to train a model}
\usage{
get_training_dataset(model_id, board = NULL)
}
\arguments{
\item{model_id}{Model name}

\item{board}{Pins board to load model from (NULL = local training board)}
}
\value{
Path to the dataset tar.gz file
}
\description{
Retrieves the exact dataset version that was used to train a model.
This ensures reproducibility by downloading the specific version from pins.
}
\examples{
\dontrun{
# Get the dataset used to train a model
dataset_path <- get_training_dataset("my_model")

# Can then use this to retrain or analyze
train_model(data_dir = dataset_path, model_id = "my_model_v2", ...)
}
}
