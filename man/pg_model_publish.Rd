% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pins.R
\name{pg_model_publish}
\alias{pg_model_publish}
\title{Publish a trained model to a pins board}
\usage{
pg_model_publish(
  model_dir,
  model_id,
  board = pg_board_user(),
  metadata = list(),
  include_metrics = TRUE,
  manifest = NULL,
  write_manifest = TRUE
)
}
\arguments{
\item{model_dir}{Directory containing Detectron2 artifacts.}

\item{model_id}{Pin/model identifier (e.g. "petrography/inclusions").}

\item{board}{Pins board (defaults to \code{\link[=pg_board_user]{pg_board_user()}}).}

\item{metadata}{Optional metadata list merged into the manifest.}

\item{include_metrics}{Whether to include \code{metrics.json} when present.}

\item{manifest}{Optional pre-built manifest. When \code{NULL}, a manifest is
derived automatically.}
}
\value{
A list containing \code{manifest}, \code{pin_meta}, and \code{bundle_path} (invisible).
}
\description{
Builds a self-describing artifact bundle (weights, config, metrics, manifest)
and uploads it to the configured board. The manifest is also written back to
the source directory for local use.
}
